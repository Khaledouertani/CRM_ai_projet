name: CI Pipeline

on:
  push:
    branches: [main, develop, feature/*]
  pull_request:
    branches: [main]

jobs:
  backend-build:
    name: Backend Build & Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./BackendDotNet

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --configuration Release --no-restore

      - name: Run Unit Tests
        run: dotnet test tests/CrmApi.Tests/CrmApi.Tests.csproj --configuration Release --no-build --verbosity normal

      - name: Run Integration Tests
        run: dotnet test tests/CrmApi.Tests/CrmApi.Tests.csproj --configuration Release --no-build --filter "FullyQualifiedName~Integration" --verbosity normal

      - name: Run Security Tests
        run: dotnet test tests/CrmApi.Tests/CrmApi.Tests.csproj --configuration Release --no-build --filter "FullyQualifiedName~Security" --verbosity normal

      - name: Run Database Tests
        run: dotnet test tests/CrmApi.Tests/CrmApi.Tests.csproj --configuration Release --no-build --filter "FullyQualifiedName~Database" --verbosity normal

      - name: Run Service Tests
        run: dotnet test tests/CrmApi.Tests/CrmApi.Tests.csproj --configuration Release --no-build --filter "FullyQualifiedName~Services" --verbosity normal

      - name: Collect Code Coverage
        run: dotnet test tests/CrmApi.Tests/CrmApi.Tests.csproj --configuration Release --no-build --collect:"XPlat Code Coverage" --settings coverlet.runsettings

      - name: Upload Coverage
        uses: actions/upload-artifact@v4
        with:
          name: backend-coverage
          path: BackendDotNet/tests/CrmApi.Tests/TestResults/**/coverage.cobertura.xml

  frontend-build:
    name: Frontend Build & Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: './frontend/package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Lint check
        run: npx tsc --noEmit

      - name: Build
        run: npm run build

      - name: Run Unit Tests
        run: npx vitest run --coverage

      - name: Upload Coverage
        uses: actions/upload-artifact@v4
        with:
          name: frontend-coverage
          path: frontend/coverage/

  e2e-tests:
    name: E2E Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright
        run: npx playwright install --with-deps chromium

      - name: Start backend
        run: |
          cd ../BackendDotNet/src/CrmApi
          dotnet run --environment Development &
          sleep 15

      - name: Start frontend
        run: |
          npm run dev -- --host 0.0.0.0 &
          sleep 10

      - name: Run Playwright tests
        run: |
          if [ -d "e2e" ] && [ "$(ls -A e2e/ 2>/dev/null)" ]; then
            npx playwright test
          else
            echo "No e2e tests found. Skipping."
          fi

      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: frontend/playwright-report/

  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Run .NET Security Scanner
        continue-on-error: true
        run: |
          dotnet tool install --global security-scan 2>/dev/null || true
          dotnet security-scan BackendDotNet/src/CrmApi/CrmApi.csproj 2>/dev/null || security-scan BackendDotNet/src/CrmApi/CrmApi.csproj 2>/dev/null || echo "Security scan tool not available for this runtime"

      - name: Run npm audit
        run: |
          cd frontend
          npm audit --audit-level=high

  dependency-scan:
    name: Dependency Scan
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Check NuGet vulnerabilities
        run: dotnet list BackendDotNet/src/CrmApi/CrmApi.csproj package --vulnerable

      - name: Check npm vulnerabilities
        run: |
          cd frontend
          npm audit
