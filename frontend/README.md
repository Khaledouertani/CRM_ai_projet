
  # Interface CRM moderne

  This is a code bundle for Interface CRM moderne. The original project is available at https://www.figma.com/design/EwNEP5BGO78XIwsgeGabSj/Interface-CRM-moderne.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  